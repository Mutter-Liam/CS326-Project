export function renderMyBoardlist(element) {
    // clear the content of the element. 
    element.innerHTML = "";

    initList(element);
}

function initList(element) {
    element.innerHTML = "My Boards"
}